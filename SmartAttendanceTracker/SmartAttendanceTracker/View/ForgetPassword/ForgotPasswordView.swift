import SwiftUI

struct ForgotPasswordView: View {
    var body: some View {
        Text("Reset your password.")
            .font(.title)
            .fontWeight(.bold)
            .padding(.top, 50)
        Spacer()
    }
}

#Preview {
    ForgotPasswordView()
}

